# Copy to .env and adjust as needed. docker-compose.yml assembles
# DATABASE_URL / REDIS_URL for the cubicle-api container from these values.

ENV=development
LOG_LEVEL=INFO

POSTGRES_DB=cubicle
POSTGRES_USER=cubicle
POSTGRES_PASSWORD=cubicle
